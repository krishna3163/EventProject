package com.company.event.user;

public enum Roles {
    // Legacy roles (kept for backward compatibility)
    ADMIN,
    USER,

    // New role-based system
    STUDENT,
    ORG_ADMIN,
    SUPER_ADMIN
}
